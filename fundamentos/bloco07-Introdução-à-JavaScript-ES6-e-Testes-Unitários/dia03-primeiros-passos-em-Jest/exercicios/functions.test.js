/*const sum = require('./sum.js');

test('sums 4 and 5 to be equal to 9', () => {
    expect(sum(4, 5)).toBe(9);
});

test('sums 0 and 0 and expect to be 0', () => {
    expect(sum(0, 0)).toBe(0);
})*/

/*const myRemove = require('./myRemove.js')
test('introduce the parameters ([1, 2, 3, 4], 3) and expected to be [1, 2, 4]', () => {
    expect(myRemove([1, 2, 3, 4], 3)).toEqual([1, 2, 4]);
})
test('introduce the parameters ([1, 2, 3, 4], 3) and expected not to be [1, 2, 3, 4]', () => {
    expect(myRemove([1, 2, 3, 4], 3)).not.toEqual([1, 2, 3, 4]);
})
test('introduce the parameters ([1, 2, 3, 4], 5) and expected to be [1, 2, 3, 4]', () => {
    expect(myRemove([1, 2, 3, 4], 5)).toEqual([1, 2, 3, 4]);
})*/

/*const myFizzBuzz = require('./myFizzBuzz')
test('receive 15 and try to divided by 3 and 5, the return must be fizzbuzz', () => {
    expect(myFizzBuzz(15)).toBe('fizzbuzz');
})
test('receive 3 and try to divided by 3 and 5, the return must be fizz', () => {
    expect(myFizzBuzz(3)).toBe('fizz');
})
test('receive 5 and try to divided by 3 and 5, the return must be buzz', () => {
    expect(myFizzBuzz(5)).toBe('buzz');
})
test('receive 7 and try to divided by 3 and 5, the return must be buzz', () =>{
    expect(myFizzBuzz(7)).toBe(7);
})*/

const functions = require('./functions')
